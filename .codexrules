# Codex 项目规则
# 此文件由项目创建，Codex 行为受此约束

## 自检规则（强制）
在执行任何汇报、展示、部署之前，必须运行 `python self_check.py` 确认所有检查通过。
不通过自检不能汇报。

## 检查项
1. DOCTYPE 不能有前导空格
2. 不能使用 Object.values
3. JS 语法必须通过 node --check
4. 所有功能函数必须存在
